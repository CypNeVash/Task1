
@{
    ViewBag.Title = "Index";
}
@model List<Blogs.Model.Account.ApplicationUser>

<table border="5">
    <tr>
        <th>Id</th>
        <th>Email</th>
        <th>UserName</th>
        <th>AccessFailedCount</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id </td>
            <td>@item.Email </td>
            <td>@item.UserName</td>
            <td>@item.AccessFailedCount</td>
        </tr>
    }
</table>
